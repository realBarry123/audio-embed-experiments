[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "audembed"
version = "0.1.0"
dependencies = [
    "matplotlib",
    "torch",
    "einops"
]